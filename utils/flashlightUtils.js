import Torch from 'react-native-torch';

export const flashlightToggle = () => {
  alert('We will make it work later');
  // Torch.switchState(true); // Turn ON
  Torch.switchState(false); // Turn ON
};
